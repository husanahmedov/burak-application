{
  "name": "burak",
  "version": "1.0.0",
  "description": "Burak Restaurant Application",
  "main": "src/server.ts",
  "scripts": {
    "start": "ts-node src/server.ts",
    "start:dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "train": "nodemon --exec ts-node src/train.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "MIT(Roger)",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^24.7.0",
    "nodemon": "^3.0.2",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "dotenv": "^16.0.0"
  }
}
